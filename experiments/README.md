# Experiments

This folder contains non-production tracks:

- `benchmarks/`: benchmarking runners and analysis scripts.
- `vllm_voxtral/`: vLLM/vllm-metal Voxtral runtime experiments.

Production daily operations are in the repo root (`voice_control.sh`) and `docs/runbooks/`.
