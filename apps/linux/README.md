# Linux Legacy Path

This folder contains the previous Whisper-first workflow scripts and helpers.

Primary location:
- `legacy_whisper/`

These files are kept for compatibility and reference; the active production path is macOS always-on (`voice_control.sh` at repo root).
